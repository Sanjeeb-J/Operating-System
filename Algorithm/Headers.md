| Header File   | Purpose                     | Common Functions / Constants                                                                                                                                                                                                                                              | Used In Programs                                                                                 |
| ------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `stdio.h`     | Input/Output Operations     | `printf()` - Print formatted output to console<br>`scanf()` - Read formatted input from user<br>`perror()` - Print system error message                                                                                                                                   | add.c, adder.c, FCFS.c, SJF.c, Priority.c, SSTF.c, LOOK.c, SCAN.c, C-SCAN.c, C-LOOK.c            |
| `stdlib.h`    | Utility & Memory Functions  | `atoi()` - Convert ASCII string to integer<br>`rand()` - Generate random number<br>`srand()` - Seed random number generator<br>`abs()` - Calculate absolute value<br>`malloc()` - Allocate dynamic memory<br>`free()` - Deallocate memory<br>`exit()` - Terminate program | add.c, adder.c, FIFO.c, LRU.c, Optimal.c, SSTF.c, LOOK.c, SCAN.c, C-SCAN.c, C-LOOK.c             |
| `time.h`      | Time & Date Functions       | `time()` - Get current system time<br>`srand(time(NULL))` - Seed RNG with current time                                                                                                                                                                                    | FIFO.c, LRU.c, Optimal.c, SSTF (random).c                                                        |
| `limits.h`    | Integer Limit Constants     | `INT_MAX` - Maximum integer value (2147483647)<br>`INT_MIN` - Minimum integer value (-2147483648)                                                                                                                                                                         | SSTF.c, Priority.c, SRT.c, findNextProcess()                                                     |
| `pthread.h`   | Multithreading & Threads    | `pthread_t` - Thread identifier type<br>`pthread_create(&thread, NULL, function, arg)` - Create new thread<br>`pthread_join(thread, NULL)` - Wait for thread completion<br>`pthread_exit(NULL)` - Terminate thread                                                        | Statistics.c (mean, median, SD)<br>Reader-Writer.c (both versions)                               |
| `semaphore.h` | Synchronization Semaphores  | `sem_t` - Semaphore type<br>`sem_init(&sem, 0, initial_value)` - Initialize semaphore<br>`sem_wait(&sem)` - Wait/lock (decrement)<br>`sem_post(&sem)` - Signal/unlock (increment)<br>`sem_destroy(&sem)` - Destroy semaphore                                              | Reader-Writer (Reader-Preference).c<br>Reader-Writer (Writer-Preference).c                       |
| `unistd.h`    | POSIX System Calls          | `sleep(seconds)` - Sleep for specified seconds<br>`fork()` - Create child process<br>`getpid()` - Get current process ID<br>`getppid()` - Get parent process ID<br>`execvp(program, args)` - Execute program                                                              | adder.c, Reader-Writer.c (both versions)<br>Process Creation examples                            |
| `sys/types.h` | System Data Types           | `pid_t` - Process ID type<br>`size_t` - Size type for memory<br>`mode_t` - File mode type                                                                                                                                                                                 | adder.c, fork examples, Process Creation                                                         |
| `sys/wait.h`  | Process Synchronization     | `wait(NULL)` - Wait for child process to complete<br>`waitpid(pid, status, options)` - Wait for specific process                                                                                                                                                          | adder.c (improved version)<br>Process examples                                                   |
| `sys/ipc.h`   | Inter-Process Communication | `key_t` - IPC key type<br>`ftok(pathname, id)` - Create unique IPC key<br>`IPC_CREAT` - Create flag<br>`IPC_RMID` - Remove flag                                                                                                                                           | Message Queue.c<br>Shared Memory.c                                                               |
| `sys/msg.h`   | Message Queue Functions     | `msgget(key, flags)` - Create/access message queue<br>`msgsnd(msgid, message, size, flags)` - Send message<br>`msgrcv(msgid, message, size, type, flags)` - Receive message<br>`msgctl(msgid, command, buffer)` - Message queue control                                   | Message Queue (Palindrome).c                                                                     |
| `sys/shm.h`   | Shared Memory Functions     | `shmget(key, size, flags)` - Create/access shared memory<br>`shmat(shmid, address, flags)` - Attach shared memory<br>`shmdt(address)` - Detach shared memory<br>`shmctl(shmid, command, buffer)` - Shared memory control                                                  | Shared Memory (String Operations).c                                                              |
| `string.h`    | String Manipulation         | `strlen(string)` - Get string length<br>`strcpy(dest, src)` - Copy string<br>`strcat(dest, src)` - Concatenate strings<br>`strcmp(str1, str2)` - Compare strings (returns 0 if equal)                                                                                     | Message Queue.c - `strcpy()`, `strcmp()`<br>Shared Memory.c - `strcpy()`, `strcat()`, `strlen()` |
| `ctype.h`     | Character Type Functions    | `toupper(char)` - Convert to uppercase<br>`tolower(char)` - Convert to lowercase<br>`isupper(char)` - Check if uppercase<br>`islower(char)` - Check if lowercase<br>`isdigit(char)` - Check if digit                                                                      | Statistics (Shared Memory).c - `toupper()`, `tolower()`, `isupper()`, `islower()`                |
| `math.h`      | Mathematical Functions      | `sqrt(value)` - Calculate square root<br>`abs(value)` - Absolute value (for double/float)                                                                                                                                                                                 | Statistics.c - `sqrt()` for Standard Deviation                                                   |